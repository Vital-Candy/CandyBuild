# CandyBuild default configuration.
# These are only the DEFAULTS offered when running `candybuild new` —
# every project gets its own copy of these values in its Candy.toml,
# which is what the build actually reads.

MIN_SDK=26
TARGET_SDK=34
BUILD_TYPE=debug
